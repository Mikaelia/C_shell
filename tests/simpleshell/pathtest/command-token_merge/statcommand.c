#include "holberton.h"
/**
 * main - stat example
 *
 * Return: Always 0.
 */
int statcommand(char *executable)
{
    struct stat st;

    stat(av[i], &st) == 0)
        {
            printf(" FOUND\n");
        }
        else
        {
            printf(" NOT FOUND\n");
        }
        i++;
    }
}
